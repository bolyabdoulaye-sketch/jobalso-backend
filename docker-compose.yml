services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: jobalso
      POSTGRES_PASSWORD: jobalso_dev_password
      POSTGRES_DB: jobalso_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: